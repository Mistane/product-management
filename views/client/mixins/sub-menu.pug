mixin sub-menu(records, pathProduct, level = 1)
  - const prefix = new Array(level + 1).join("-")
  ul
    each item in records
      a(href=`${pathProduct}/${item.slug}` style="display:block;") #{prefix}#{item.title}
      if item.children
        +sub-menu(item.children, pathProduct, level + 1)
